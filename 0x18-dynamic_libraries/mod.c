#include <stdlib.h>
#include <stdio.h>

/**
 * mod - modulus of two integers
 * @a: the first integer
 * @b: the second integer
 * Return: remainder after division
 */
int mod(int a, int b)
{
        return(a % b);
}
